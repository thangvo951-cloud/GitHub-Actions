name: Professional_CI_CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Bước 1: Lấy mã nguồn từ repo
      - name: Checkout Code
        uses: actions/checkout@v4

      # Bước 2: Thiết lập môi trường
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      # Bước 3: Restore
      - name: Restore
        run: dotnet restore GitHub-Actions.sln

      # Bước 4: Build
      - name: Build
        run: dotnet build GitHub-Actions.sln --configuration Release --no-restore

      # Bước 5: Test
      - name: Test
        run: dotnet test GitHub-Actions.sln --no-build --verbosity normal
  deploy:
    needs: build-and-test
    runs-on: ubuntu-latestgit
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    
    steps:
      # Bước 6: Thông báo triển khai
      - name: Notify Deployment
        run: echo "Deploying to server completed!"